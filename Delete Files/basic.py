# using the os module 
# Module (like a code library) is a file written by another programmer that generally has a functions we can use.
# Syntax: 
"""
import os
os.remove(filename)
"""

import os
os.remove("../Files/abc.txt")
print("Operation performed!")